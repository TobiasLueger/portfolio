import React from "react";

const Project = (props) => {
  const { projectTitle } = props;
  return (
    <div>
      {/* TODO: make project json */}
      <h1>{projectTitle}</h1>
      <h2>{/* subheader */}</h2>
      {/* img */}
      {/* text */}

      {/* sidebar */}
      {/* datum */}
      {/* chips */}
      {/* img */}
    </div>
  );
};

export default Project;
