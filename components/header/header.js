const _ = {
	defaults: {
		componentSelector: '.header',
		pluginOptions: {}
	}
};

const o = _.defaults;

function  bindListeners() {
	console.log("header init");
}

export function init() {
	bindListeners();
    
         
}

